server:
  port: 8081

spring:
  application:
    name: techfood-clientes

  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5433/techfood-clientes
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

